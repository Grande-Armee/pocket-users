export enum UserRoutingKey {
  createUser = 'pocket.users.users.createUser',
  findUser = 'pocket.users.users.findUser',
  updateUser = 'pocket.users.users.updateUser',
  removeUser = 'pocket.users.users.removeUser',
  loginUser = 'pocket.users.auth.loginUser',
  setNewPassword = 'pocket.users.auth.setNewPassword',
}
